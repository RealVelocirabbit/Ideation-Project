// const fs = require('fs');
// const path = require('path');
// const mockDB = require('../server/DBTest/test.js')

// const testJsonFile = path.resolve(__dirname, '../server/DBTest/notes.test.json');

// describe('db tests') {
//   beforeAll((done) => {
//     fs.writeFile(testJsonFile, JSON.stringify([]), () => {
//       mockDB.reset();
//       done();
//     });
//   });

//   afterAll((done) => {
//     fs.writeFile(testJsonFile, JSON.stringify([]), done);
//   });
// }
